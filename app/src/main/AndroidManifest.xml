<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nbamir.sda1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Accounts.SignInActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NavigationDrawer.NavigationDrawer" />
        <activity android:name=".TemplatePattern.RequestedEventsActivity" />
        <activity android:name=".BookmarksActivity" />
        <activity android:name=".EventBoard.EventBoardActivity" />
        <activity android:name=".Accounts.SignUpActivity" />
        <activity android:name=".EventMaker.AddEventActivity" />
        <activity android:name=".EventMaker.EventPageActivity"></activity>
    </application>

</manifest>